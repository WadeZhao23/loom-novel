# 立项即铺底(一期)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 第一章不再漂移——书名进流水线 prompt、占位模板不再冒充设定、建书即收一句话设定/平台并把 AI 铺底稿编排进主流程。

**Architecture:** 引擎侧在 `parse.py` 落一组占位判定/剥离纯函数,`agents.py` 的 knowledge 组装与 `_build_user_prompt` 消费它+注入书名;流程侧 `Config.idea` + `scaffold.init(idea, platform)` 收前置输入,`draft.draft_brain` 回退读 idea,`project_state` 暴露 `brain_ready`,webui 建书框/旅程卡/织章拦截三处编排现有 `/api/brain/draft`。

**Tech Stack:** Python 3.11+(tomllib)、FastAPI、pytest(fixtures 在 `tests/conftest.py`:`project` + `FakeBackend`)、原生 JS webui(无构建步骤)。

**Spec:** `docs/design/proposals/立项即铺底.md` §1–3(§4 二期不在本计划)。

## Global Constraints

- 测试命令一律 `.venv/bin/python -m pytest`(项目自带 venv;不要用裸 `pytest`)。
- **工作区有用户未提交的改动**(`loom/server.py`、`loom/webui/*`、`packaging/loom.spec` 的 `/api/version` 等)——**绝不 `git add -A` / `git add -u`**,每次只 add 你本任务改的文件。碰到同文件时在既有改动之上叠加,不要回退它们。
- golden 快照(`tests/golden/pipeline_v1.json`)重生成后必须人工审 diff 再提交,不许盲刷(文件头注释的规矩)。
- 注释写「代码说不出的约束」,风格从众(中文、紧凑);UI 文案全中文。
- 红线:learn 只学手改(本计划不碰);draft 自动动作只覆盖空白;立项卡 loom 不自动写——平台行是作者在建书框选的,loom 只代为落盘,属前置输入。
- Windows CI 也跑测试:测试里读写文件一律 `encoding="utf-8"`;不要在断言里依赖路径分隔符。

---

### Task 1: 占位判定/剥离纯函数(parse.py)+ draft 复用

**Files:**
- Modify: `loom/parse.py`(文件末尾追加一节)
- Modify: `loom/draft.py:77-84`(`_is_blank_or_template` 改用共享常量)
- Test: `tests/test_placeholder.py`(新建)

**Interfaces:**
- Produces: `parse.PLACEHOLDER_MARKS: tuple[str, ...]`;`parse.strip_placeholder_hints(text: str) -> str`;`parse.is_substantive(text: str) -> bool`。后续 Task 3(knowledge 过滤)、Task 8(brain_ready)都消费这三个。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_placeholder.py
"""占位判定/剥离:出厂模板的「填写说明」不冒充书的设定(立项即铺底 §1)。"""
from loom.parse import PLACEHOLDER_MARKS, is_substantive, strip_placeholder_hints


def test_strip_drops_fullline_hints_keeps_content():
    text = "## 分区\n（占位示例:玄幻 · 东方玄幻。填你投稿的那个分区。）\n平台:起点\n(占位示例,换成你自己的。)"
    out = strip_placeholder_hints(text)
    assert "占位示例" not in out
    assert "平台:起点" in out
    assert "## 分区" in out


def test_strip_keeps_inline_mentions():
    # 作者正文里恰好写到「占位示例」四个字(非整行括注)→ 不是提示行,不许剥
    text = "他说这只是个占位示例罢了。"
    assert strip_placeholder_hints(text) == text


def test_substantive_false_for_template_like_text():
    # 出厂模板剥完只剩 标题/引用注释/空章行 → 无实质
    text = "# 卡章纲\n\n> 一章一句话的脊柱。\n> (占位示例,换成你自己的。)\n\n- 第1章:\n- 第2章:\n"
    assert is_substantive(text) is False


def test_substantive_true_when_row_filled():
    text = "# 卡章纲\n\n- 第1章:崇祯睁眼,先杀魏忠贤,章末锦衣卫围殿。\n- 第2章:\n"
    assert is_substantive(text) is True


def test_substantive_true_for_plain_line():
    assert is_substantive("平台:起点") is True


def test_marks_cover_draft_side():
    # draft 写侧防覆盖与读侧过滤共用同一份标记
    assert "占位示例" in PLACEHOLDER_MARKS and "待填充" in PLACEHOLDER_MARKS
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_placeholder.py -v`
Expected: FAIL — `ImportError: cannot import name 'PLACEHOLDER_MARKS'`

- [ ] **Step 3: 实现(parse.py 末尾追加)**

```python
# ── 占位模板判定(立项即铺底):出厂模板里「写给作者的填写说明」——
# draft 写侧防覆盖(_is_blank_or_template)与 knowledge 读侧过滤共用这份标记,保证同一语义。
PLACEHOLDER_MARKS = ("占位示例", "待 seed", "待填充")

# 提示行=整行被（）/() 括起且含占位标记;正文里顺嘴提到标记词的句子不算,不许误剥。
_HINT_LINE_RE = re.compile(
    r"^\s*[（(].*(?:" + "|".join(PLACEHOLDER_MARKS) + r").*[)）]\s*$")
# 空章行:「- 第N章:」冒号后没内容(卡章纲去毒后的骨架行,不算实质内容)
_EMPTY_ROW_RE = re.compile(r"^-\s*第\S{0,6}章[:：]\s*$")


def strip_placeholder_hints(text: str) -> str:
    """剥掉占位提示行。真内容与提示混排的文件(如立项卡的平台行 + 各格括注)只丢提示、留内容
    ——不能按「文件含标记」整份误杀。"""
    return "\n".join(l for l in text.splitlines() if not _HINT_LINE_RE.match(l))


def is_substantive(text: str) -> bool:
    """剥占位提示后还有没有实质内容。标题/引用注释/空章行不算——出厂模板剥完只剩这些,
    不该冒充设定进 prompt(读侧过滤与 brain_ready 判定共用)。"""
    for line in strip_placeholder_hints(text).splitlines():
        s = line.strip()
        if not s or s.startswith("#") or s.startswith(">") or _EMPTY_ROW_RE.match(s):
            continue
        return True
    return False
```

注意 `parse.py` 顶部已 `import re`,不必重复。

`loom/draft.py` 的 `_is_blank_or_template` 最后一行改为共享常量(行为不变):

```python
from .parse import PLACEHOLDER_MARKS  # 文件顶部 import 区,与既有 from .parse 合并

def _is_blank_or_template(path: Path) -> bool:
    """文件缺失 / 空 / 还是占位模板(含占位标记)→ 可安全覆盖;否则保留作者内容。"""
    if not path.exists():
        return True
    text = path.read_text(encoding="utf-8")
    if not text.strip():
        return True
    return any(m in text for m in PLACEHOLDER_MARKS)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_placeholder.py tests/test_brain_dirs.py -v`
Expected: 全 PASS(test_brain_dirs 覆盖 draft 写侧,确认重构没改行为)

- [ ] **Step 5: Commit**

```bash
git add loom/parse.py loom/draft.py tests/test_placeholder.py
git commit -m "feat(parse): 占位判定/剥离纯函数——提示行可剥、实质内容可判,draft 写侧共用标记"
```

---

### Task 2: 卡章纲模板去毒(预填示例行改空)

**Files:**
- Modify: `loom/templates/外置大脑/卡章纲.md`
- Test: `tests/test_placeholder.py`(追加一个用例)

**Interfaces:**
- Produces: 新书的 `外置大脑/卡章纲.md` 满足 `is_substantive(strip_placeholder_hints(text)) == False`。Task 3 的过滤、Task 8 的 brain_ready 依赖这一点。

- [ ] **Step 1: 写失败测试(追加到 tests/test_placeholder.py)**

```python
def test_fresh_project_card_is_not_substantive(project):
    # 卡章纲模板去毒:预填的通用金手指示例行(会被大纲师当真纲执行)必须改空
    text = (project / "外置大脑/卡章纲.md").read_text(encoding="utf-8")
    assert is_substantive(text) is False
    assert "濒死" not in text   # 通用金手指剧本的预填行必须绝迹(写法示范只许住在 > 注释里)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_placeholder.py::test_fresh_project_card_is_not_substantive -v`
Expected: FAIL — 模板预填行「主角在最糟处境登场(濒死/被废/被冤)」是实质内容

- [ ] **Step 3: 改模板(整文件替换为下面内容)**

```markdown
# 卡章纲

> 一章一句话的脊柱。大纲师 Agent 写第 N 章时,查第 N 行知道这章要完成什么、上一章结尾的钩子是什么。
> 这是跨章剧情连续性的主线(v0.1 跨章记忆靠它 + 上一章正文)。
> 你 learn 一章后,Loom 会在该章行下自动补一个缩进的「- [AI回顾] 摘要/伏笔」子块(这章实际写成了什么、埋/推/收了哪些伏笔),帮后面几十章接得住前情。它可手改可删,改不改都不影响你手写的规划行。
> 写法示范:「- 第1章:主角在最糟处境登场,金手指初现,章末抛一个让人想翻下一章的钩子」——每行=这章完成什么+章末钩子,前三章最好有小爽点闭环。
> (占位示例,换成你自己的;也可以在建书后用「AI 铺设定底稿」按书名起草前 5 章。)

- 第1章:
- 第2章:
- 第3章:
- 第4章:
- 第5章:
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_placeholder.py -v`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add loom/templates/外置大脑/卡章纲.md tests/test_placeholder.py
git commit -m "fix(templates): 卡章纲去毒——预填示例行改空,格式示范挪进注释(漂移直接凶手)"
```

---

### Task 3: knowledge 组装占位过滤(agents._read_file_items)

**Files:**
- Modify: `loom/agents.py:224-238`(`_read_file_items`)
- Test: `tests/test_knowledge_filter.py`(新建)

**Interfaces:**
- Consumes: `parse.strip_placeholder_hints` / `parse.is_substantive`(Task 1)
- Produces: `_read_file_items` 返回的 (rel, text) 中 text 已剥提示行、无实质内容的文件不在列。`_knowledge_items`/`_knowledge_prompt`/`_read_files`(gate 用)自动继承。

**已知副作用(有意):** `_read_file_items` 同时供续跑签名——含提示行的老书(立项卡没删括注)下次写章会从设定师全量重跑一次(一次性重计费,与改世界观同级别,可接受)。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_knowledge_filter.py
"""knowledge 组装的占位过滤:占位模板是写给作者的填写说明,不是书的设定(立项即铺底 §1)。"""
from loom.agents import _knowledge_items
from loom.fsutil import atomic_write_text


def test_fresh_project_card_not_fed_to_outliner(project):
    # 新书卡章纲还是空骨架 → 大纲师读不到它(否则空行/注释冒充章纲)
    _, items = _knowledge_items(project, 1, "大纲师")
    assert all("卡章纲" not in rel for rel, _ in items)


def test_filled_card_is_fed(project):
    card = project / "外置大脑/卡章纲.md"
    text = card.read_text(encoding="utf-8").replace("- 第1章:", "- 第1章:崇祯睁眼,先杀魏忠贤,章末锦衣卫围殿。")
    atomic_write_text(card, text)
    _, items = _knowledge_items(project, 1, "大纲师")
    got = {rel: t for rel, t in items}
    assert "外置大脑/卡章纲.md" in got
    assert "魏忠贤" in got["外置大脑/卡章纲.md"]


def test_project_card_hints_stripped_but_platform_kept(project):
    # 立项卡真内容(平台行)与占位括注混排:括注剥掉、平台行保留,不许整份误杀
    _, items = _knowledge_items(project, 1, "设定师")
    got = {rel: t for rel, t in items}
    assert "外置大脑/立项卡.md" in got
    assert "占位示例" not in got["外置大脑/立项卡.md"]
    assert "平台:起点" in got["外置大脑/立项卡.md"]


def test_writer_still_gets_fingerprint(project):
    # 中性默认指纹是真内容(写手的灵魂),过滤绝不能把它剥没
    _, items = _knowledge_items(project, 1, "写手")
    assert any(rel.endswith("写作指纹.md") for rel, _ in items)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_knowledge_filter.py -v`
Expected: `test_fresh_project_card_not_fed_to_outliner` 与 `test_project_card_hints_stripped_but_platform_kept` FAIL(现在原文直喂);另两个可能已 PASS

- [ ] **Step 3: 实现(替换 _read_file_items 整个函数)**

```python
def _read_file_items(project_root: Path, rels: list[str], progress: Progress) -> list[tuple[str, str]]:
    """reads 清单 → [(展示名, 文本)]。knowledge 拼装与续跑签名共用这一份,保证同源。
    占位提示行不进 prompt;剥完无实质内容的文件(出厂模板)整份跳过——
    占位模板是写给作者的填写说明,不是书的设定(docs/design/proposals/立项即铺底.md §1)。"""
    from .parse import is_substantive, strip_placeholder_hints
    items: list[tuple[str, str]] = []

    def _add(name: str, raw: str) -> None:
        text = strip_placeholder_hints(raw)
        if is_substantive(text):
            items.append((name, text))

    for rel in rels:
        p = project_root / rel
        if p.is_dir():
            # 目录型 reads(如 skills/题材):读其中所有 .md(跳过 README,排序保证签名稳定)
            for f in sorted(p.glob("*.md")):
                if f.stem != "README":
                    _add(f"{rel}/{f.name}", f.read_text(encoding="utf-8"))
        elif p.exists():
            _add(rel, p.read_text(encoding="utf-8"))
        else:
            progress(events.warn(f"跳过缺失文件 {rel}"))
    return items
```

- [ ] **Step 4: 跑测试确认通过 + 相关回归**

Run: `.venv/bin/python -m pytest tests/test_knowledge_filter.py tests/test_resume_sig.py tests/test_hardfacts.py -v`
Expected: 全 PASS。若 `tests/test_golden_pipeline.py` 此刻跑会 FAIL——正常,Task 5 统一重生成,先别动。

- [ ] **Step 5: Commit**

```bash
git add loom/agents.py tests/test_knowledge_filter.py
git commit -m "feat(agents): knowledge 占位过滤——提示行剥离、无实质文件整份跳过,不再冒充设定"
```

---

### Task 4: 书名注入流水线 prompt + 续跑签名

**Files:**
- Modify: `loom/agents.py:480-500`(`_build_user_prompt`)、`:532`(`_cfg_bits`)、`:563-565` 与 `:716-718`(两处调用点)
- Test: `tests/test_title_in_prompt.py`(新建)

**Interfaces:**
- Consumes: `config.Config.title`(既有)
- Produces: `_build_user_prompt(..., book_title: str = "")` 关键字参数;user prompt 首行 `# 你要写的是《{title}》第 N 章。`;`_cfg_bits` 含 `"title"` 键(改书名 → 签名失配 → 全量重跑)。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_title_in_prompt.py
"""书名注入:模型必须知道这本书叫什么(立项即铺底 §1 根因一)。"""
from loom.agents import regen_outline, run_pipeline
from loom.config import load_config, save_config

_OUTLINE_RUN = ["锚点:崇祯睁眼在乾清宫。", "分镜一:验身。分镜二:召王承恩。章末钩(危机迫近)。"]


class ScriptBackend:
    """按脚本顺序吐产出,记录 (system, user);耗尽即炸。"""

    def __init__(self, script):
        self.script = list(script)
        self.calls = []

    def complete(self, system, user, *, max_chars=None, on_chunk=None):
        assert self.script, "脚本耗尽:调用次数超出预期"
        out = self.script.pop(0)
        self.calls.append((system, user))
        if on_chunk and out:
            on_chunk(out)
        return out


def test_title_in_every_step_prompt(project):
    be = ScriptBackend(_OUTLINE_RUN)
    regen_outline(project, 1, be, load_config(project))
    assert len(be.calls) == 2  # 设定师 + 大纲师
    for _, user in be.calls:
        assert user.startswith("# 你要写的是《测试书》第 1 章。")


_PROSE = ("寅时三刻,铜锣未响。崇祯睁开眼,乾清宫的帐顶陈旧而熟悉。"
          "他记得煤山那棵歪脖子树,记得白绫勒进脖颈的凉,也记得城破前夜没人来救驾。"
          "这一次,他先要魏忠贤的命。")   # ≥40 实字:过 chapter_profile(100) 的终稿闸


def test_title_change_invalidates_resume(project):
    full = ["锚点:崇祯睁眼在乾清宫,阉党当政。", "分镜一:验身。分镜二:召王承恩。章末钩(危机迫近)。",
            _PROSE, _PROSE + "补一句悬念。", _PROSE + "收束更冷。", "煤山"]
    cfg = load_config(project)
    cfg.gate_rounds = 0          # 关 gate:免去质检/去AI味的额外调用,脚本可控
    cfg.chapter_chars = 100      # 终稿闸降到 max(40, 12)=40 实字,测试文本可控
    save_config(project, cfg)
    cfg = load_config(project)
    run_pipeline(project, 1, ScriptBackend(list(full)), cfg)

    # 原样续跑:五棒签名全命中、零工序调用;标题重生成是既有行为,恒有且仅有这一次
    idle = ScriptBackend(["煤山"])
    run_pipeline(project, 1, idle, cfg)
    assert len(idle.calls) == 1
    assert "# 你要写的是" not in idle.calls[0][1]   # 这唯一一次是标题生成,不是工序

    cfg.title = "换了个书名"
    save_config(project, cfg)
    cfg = load_config(project)
    redo = ScriptBackend(list(full))   # 大纲师有细纲文件走 WYSIWYG 旁路,脚本富余没关系(不要求耗尽)
    run_pipeline(project, 1, redo, cfg)
    assert len(redo.calls) >= 2, "改书名必须让续跑签名失配、从设定师重跑"
    assert "《换了个书名》" in redo.calls[0][1]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_title_in_prompt.py -v`
Expected: FAIL — prompt 首行是「# 你要写的是第 1 章。」,无书名
(契约是三点:每棒 prompt 有《书名》头、全命中续跑只剩标题那一次调用、改书名后从设定师重跑。若 resume 用例在别处炸,先看 `tests/test_resume_sig.py` 的既有写法对齐夹具,契约不变。)

- [ ] **Step 3: 实现**

`_build_user_prompt` 签名与首行(`agents.py:480-484`):

```python
def _build_user_prompt(chapter_n: int, role: str, agent: Agent, knowledge: str,
                       prev: str, workspace: list[tuple[str, str]], hardfacts: str = "",
                       target_chars: int = 0, *, chapter_target: int = 0, wants_prev: bool = False,
                       wants_hardfacts: bool = False, book_title: str = "") -> str:
    # 书名必进 prompt:没有它,模型对「这本书是什么」一无所知(第一章漂移的根因一)
    head = (f"# 你要写的是《{book_title}》第 {chapter_n} 章。" if book_title.strip()
            else f"# 你要写的是第 {chapter_n} 章。")
    parts = [head]
```

`run_pipeline` 内 `_cfg_bits`(`agents.py:532`):

```python
    # title 进签名:改书名 → 设定师起全量重跑,绝不拿旧书名的半截稿续跑
    _cfg_bits = {"chapter_chars": config.chapter_chars, "gate_rounds": config.gate_rounds,
                 "title": config.title}
```

两处调用点(`agents.py:563` 与 `:716`)各加一个实参:

```python
            user_prompt = _build_user_prompt(chapter_n, role, agent, knowledge, prev, workspace, hardfacts,
                                             target_chars=max_chars, chapter_target=config.chapter_chars,
                                             wants_prev=spec.wants_prev, wants_hardfacts=spec.wants_hardfacts,
                                             book_title=config.title)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_title_in_prompt.py tests/test_resume_sig.py tests/test_pipeline_title.py -v`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add loom/agents.py tests/test_title_in_prompt.py
git commit -m "feat(agents): 书名注入五棒 prompt + 续跑签名——模型终于知道这本书叫什么"
```

---

### Task 5: golden 快照重生成(人工审 diff)

**Files:**
- Modify: `tests/golden/pipeline_v1.json`(工具重生成)

- [ ] **Step 1: 确认 golden 现在红**

Run: `.venv/bin/python -m pytest tests/test_golden_pipeline.py -v`
Expected: FAIL(prompt 变了:书名头 + knowledge 过滤)

- [ ] **Step 2: 重生成**

Run: `LOOM_GOLDEN_WRITE=1 .venv/bin/python -m pytest tests/test_golden_pipeline.py -v`
Expected: PASS(写入新快照)

- [ ] **Step 3: 人工审 diff(不许盲刷)**

Run: `git diff tests/golden/pipeline_v1.json | head -200`
逐段确认改动**只有**两类:① 每棒 user prompt 首行多了 `《测试书》`;② knowledge 里出厂占位内容消失(立项卡括注行、空卡章纲整份)。出现任何其他 diff(事件序列、落盘文件、max_chars)→ 停下排查,不许提交。

- [ ] **Step 4: 全量回归**

Run: `.venv/bin/python -m pytest -q`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add tests/golden/pipeline_v1.json
git commit -m "test(golden): 重生成 pipeline 快照——书名头 + 占位过滤(diff 已人工审)"
```

---

### Task 6: Config.idea + scaffold 收 idea/platform

**Files:**
- Modify: `loom/config.py:15-24`(Config)、`:59-68`(load)、`:137-159`(save)
- Modify: `loom/scaffold.py:59-94`(`init`)
- Test: `tests/test_create_seed.py`(新建)

**Interfaces:**
- Produces: `Config.idea: str = ""`;`scaffold.init(name, parent=None, genre=None, idea="", platform="")`。Task 7(draft 回退)、Task 8(server create)消费。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_create_seed.py
"""建书前置输入:一句话设定进 loom.toml(AI 铺底稿用),平台进立项卡平台行(违禁词基线用)。"""
from loom.config import Config, load_config, save_config
from loom.scaffold import init as scaffold_init


def test_idea_and_platform_land(tmp_path):
    root = scaffold_init("崇祯书", parent=tmp_path, idea='重生成崇祯,开局"砍"魏忠贤', platform="番茄")
    assert load_config(root).idea == "重生成崇祯,开局'砍'魏忠贤"   # 双引号净化成单引号,守 toml 单行字符串
    card = (root / "外置大脑/立项卡.md").read_text(encoding="utf-8")
    assert "平台:番茄" in card and "平台:起点" not in card


def test_defaults_untouched(tmp_path):
    root = scaffold_init("素书", parent=tmp_path)
    assert load_config(root).idea == ""
    assert "平台:起点" in (root / "外置大脑/立项卡.md").read_text(encoding="utf-8")


def test_idea_survives_save_config_roundtrip(tmp_path):
    root = scaffold_init("留存书", parent=tmp_path, idea="一句话设定")
    cfg = load_config(root)
    save_config(root, cfg)                       # 设置页保存后端会整写 toml,idea 不许被冲掉
    assert load_config(root).idea == "一句话设定"
    cfg2 = load_config(scaffold_init("空书", parent=tmp_path))
    save_config(tmp_path / "空书", cfg2)
    assert "idea" not in (tmp_path / "空书" / "loom.toml").read_text(encoding="utf-8")  # 没填就不写行,toml 干净
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_create_seed.py -v`
Expected: FAIL — `init() got an unexpected keyword argument 'idea'`

- [ ] **Step 3: 实现**

`config.py` — Config 加字段(`title` 行后):

```python
    title: str = "我的第一本书"
    idea: str = ""                     # 建书时的一句话设定:AI 铺设定底稿(draft)的种子;空=没填
```

`load_config` 的 return 里加:

```python
        title=novel.get("title", "我的第一本书"),
        idea=novel.get("idea", ""),
```

`save_config` — `[novel]` 段(没填不写行,保持 toml 干净):

```python
    idea_line = f'idea  = "{cfg.idea}"\n' if cfg.idea else ""
    ...
        "[novel]\n"
        f'title = "{cfg.title}"\n'
        f'{idea_line}'
        f'"章节字数" = {int(cfg.chapter_chars)}\n\n'
```

`scaffold.py` — `init` 签名与落盘(在「loom.toml 填上书名」一段扩展;文件顶部加 `import re`、从 `.paths` 的 import 行补 `PROJECT_CARD_REL`):

```python
def init(name: str, parent: Path | None = None, genre: str | None = None,
         idea: str = "", platform: str = "") -> Path:
    """在 parent(默认当前目录)下建一个名为 name 的项目骨架,返回项目根路径。

    genre 命中题材库时,只把那一份题材速查拷进项目(没选/拼错则不拷,行为与不选一致)。
    idea(一句话设定)存 loom.toml 供 AI 铺底稿;platform 写立项卡平台行——都是作者在建书框
    填的,loom 只代为落盘(立项卡「从不自动写」红线不涉:内容所有权在作者)。
    """
```

「loom.toml 填上书名」处替换为:

```python
    # loom.toml 填上书名;一句话设定紧随 title 落行(净化成单行、双引号转单,守 toml 基本字符串)
    toml = target / "loom.toml"
    content = toml.read_text(encoding="utf-8").replace("__TITLE__", name)
    clean = " ".join(idea.split()).replace('"', "'")
    if clean:
        content = content.replace(f'title = "{name}"\n', f'title = "{name}"\nidea  = "{clean}"\n', 1)
    atomic_write_text(toml, content)

    # 平台写进立项卡的可解析行(违禁词自检读它定基线)
    if platform.strip():
        card = target / PROJECT_CARD_REL
        atomic_write_text(card, re.sub(r"^平台:.*$", f"平台:{platform.strip()}",
                                       card.read_text(encoding="utf-8"), count=1, flags=re.M))
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_create_seed.py tests/test_usecases.py tests/test_sensitive_scan.py -v`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add loom/config.py loom/scaffold.py tests/test_create_seed.py
git commit -m "feat(create): 建书收一句话设定/平台——idea 进 loom.toml,平台行落立项卡(前置输入)"
```

---

### Task 7: draft 起草——idea 回退 + 题材自适应 + 硬设定关键词补词

**Files:**
- Modify: `loom/draft.py:34-64`(`_DRAFT_SYSTEM`)、`:87-99`(`draft_brain`)
- Modify: `loom/agents.py:263-264`(`_HARDFACT_KW`)
- Test: `tests/test_draft_seed.py`(新建)、`tests/test_hardfacts.py`(追加)

**Interfaces:**
- Consumes: `Config.idea`(Task 6)
- Produces: `draft_brain(root, "", backend)` 空 idea 时自动用 `cfg.idea`;`_HARDFACT_KW` 含 `"时代"`、`"优势"`(历史/都市题材的「时代格局/主角优势」小节吃到逐字旁路)。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_draft_seed.py
"""draft 起草:空 idea 回退读建书时存的 Config.idea;prompt 按题材自适应(历史题材不硬凑力量体系)。"""
from loom.config import load_config, save_config
from loom.draft import _DRAFT_SYSTEM, draft_brain
from conftest import FakeBackend, const

_OK = ("===世界观===\n## 一句话定位\n末代皇帝的翻盘局。\n===人物卡===\n## 主角 · 朱由检\n- 核心欲望:活下去。\n"
       "===卡章纲===\n- 第1章:睁眼,杀魏忠贤,章末锦衣卫围殿。")


def test_empty_idea_falls_back_to_config(project):
    cfg = load_config(project)
    cfg.idea = "重生成崇祯,开局砍魏忠贤"
    save_config(project, cfg)
    be = FakeBackend(const(_OK))
    draft_brain(project, "", be)
    assert "重生成崇祯,开局砍魏忠贤" in be.calls[0][1]


def test_explicit_idea_wins(project):
    cfg = load_config(project)
    cfg.idea = "存的旧设定"
    save_config(project, cfg)
    be = FakeBackend(const(_OK))
    draft_brain(project, "现填的新设定", be)
    assert "现填的新设定" in be.calls[0][1]
    assert "存的旧设定" not in be.calls[0][1]


def test_prompt_is_genre_adaptive():
    # 系统提示词不再把「力量体系/金手指」焊死为必填——历史/都市题材有替代小节
    assert "时代格局" in _DRAFT_SYSTEM
    assert "主角优势" in _DRAFT_SYSTEM
```

追加到 `tests/test_hardfacts.py`(文件末尾;先读该文件开头,沿用它现成的项目/写文件工具函数——下面用最朴素写法,若文件里有现成 helper 就换用):

```python
def test_hardfacts_cover_era_and_edge_sections(project):
    from loom.agents import _hardfacts_for
    d = project / "外置大脑/世界观"
    d.mkdir(parents=True, exist_ok=True)
    (d / "时代格局.md").write_text("# 时代格局\n\n天启七年,阉党当政,辽东糜烂。", encoding="utf-8")
    (d / "主角优势.md").write_text("# 主角优势\n\n先知三百年史;代价:改史则记忆漫漶。", encoding="utf-8")
    facts = _hardfacts_for(project, lambda e: None)
    assert "天启七年" in facts and "先知三百年史" in facts
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_draft_seed.py tests/test_hardfacts.py -v`
Expected: 新用例 FAIL(回退不存在、prompt 无「时代格局」、关键词不含「时代/优势」)

- [ ] **Step 3: 实现**

`draft.py` — `draft_brain` 开头(cfg 加载后)加回退:

```python
    cfg = load_config(project_root)
    title = (cfg.title or "").strip() or "（未命名）"
    idea = idea.strip() or cfg.idea      # 没现填就用建书时存的一句话设定(Config.idea)
    genre = _genre(project_root)
```

(后文 `f"作者的一句话设定:{idea.strip() or ...}"` 里的 `idea.strip()` 改成 `idea`,已 strip 过。)

`_DRAFT_SYSTEM` 的 `===世界观===` 段替换为(其余段不动):

```
===世界观===
## 一句话定位
（情绪基调 + 核心冲突）
## 力量体系
（按题材取舍:玄幻/修仙/西幻/克苏鲁等超自然题材才写本节——体系名 + 7~9 个等级,每级有可见代价/异化;\
历史/都市/现实题材把本节标题改成「时代格局」——年代/政局或行业格局/主角所处阶层与规则）
## 金手指
（超自然题材:类型 / 核心功能 / 触发条件 / 至少一种硬代价 / 敌人怎么反制 / 升级路径;\
写实题材把本节标题改成「主角优势」——信息差/先知/权柄/资源,同样必须有硬代价与反制）
## 地理 / 势力
（安全区、危险区、对立势力各一两条）
## 冰山真相
（这个世界藏着的终极秘密一句话,整本书的长线）
```

`agents.py` `_HARDFACT_KW` 补两词(注释同步):

```python
# ……只圈"规则 + 专名"(境界阶梯/金手指代价/地名势力;历史/都市题材的 时代格局/主角优势 同级)……
_HARDFACT_KW = ("力量", "体系", "境界", "等级", "修为", "实力", "修炼", "金手指",
                "地理", "势力", "阵营", "地图", "区域", "时代", "优势")
```

- [ ] **Step 4: 跑测试确认通过 + golden 复核**

Run: `.venv/bin/python -m pytest tests/test_draft_seed.py tests/test_hardfacts.py tests/test_golden_pipeline.py -v`
Expected: 全 PASS(golden 的测试项目世界观为空,关键词扩词不该影响快照;若红了按 Task 5 流程重审重生成)

- [ ] **Step 5: Commit**

```bash
git add loom/draft.py loom/agents.py tests/test_draft_seed.py tests/test_hardfacts.py
git commit -m "feat(draft): 空 idea 回退 Config.idea + 起草题材自适应;硬设定关键词补 时代/优势"
```

---

### Task 8: server——create 收 idea/platform + state.brain_ready

**Files:**
- Modify: `loom/draft.py`(末尾加 `brain_ready`)
- Modify: `loom/usecases.py:247-278`(`project_state`)
- Modify: `loom/server.py:73-76`(`CreateBody`)、`:101-109`(`create_project`)
- Test: `tests/test_brain_ready.py`(新建)

**Interfaces:**
- Consumes: `parse.is_substantive`(Task 1)、`scaffold.init(idea=, platform=)`(Task 6)
- Produces: `draft.brain_ready(project_root: Path) -> bool`;`project_state()` 响应多 `"brain_ready": bool`;`POST /api/project/create` 接受 `idea`/`platform` 字段。webui(Task 9-11)消费。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_brain_ready.py
"""brain_ready:世界观/人物/卡章纲任一有实质内容才算铺过底(旅程卡完成判定 + 织章拦截共用)。"""
from fastapi.testclient import TestClient

from loom.draft import brain_ready
from loom.fsutil import atomic_write_text
from loom.server import app
from loom.usecases import project_state


def test_fresh_project_not_ready(project):
    assert brain_ready(project) is False


def test_filled_card_row_makes_ready(project):
    card = project / "外置大脑/卡章纲.md"
    atomic_write_text(card, card.read_text(encoding="utf-8").replace("- 第1章:", "- 第1章:睁眼杀魏忠贤。"))
    assert brain_ready(project) is True


def test_world_dir_file_makes_ready(project):
    d = project / "外置大脑/世界观"
    d.mkdir(parents=True, exist_ok=True)
    atomic_write_text(d / "时代格局.md", "# 时代格局\n\n天启七年,阉党当政。")
    assert brain_ready(project) is True


def test_growth_file_alone_does_not_count(project):
    # 成长档案是 learn 的 AI 自留地,不算作者铺过底
    d = project / "外置大脑/人物"
    d.mkdir(parents=True, exist_ok=True)
    atomic_write_text(d / "成长档案.md", "## 王承恩 [AI补充·第1章]\n- 提刀护驾。")
    assert brain_ready(project) is False


def test_state_and_create_endpoint(tmp_path):
    c = TestClient(app)
    r = c.post("/api/project/create", json={"name": "崇祯书", "parent": str(tmp_path),
                                            "idea": "重生成崇祯", "platform": "番茄"})
    assert r.status_code == 200
    d = r.json()
    assert d["brain_ready"] is False
    assert "平台:番茄" in (tmp_path / "崇祯书" / "外置大脑/立项卡.md").read_text(encoding="utf-8")
    assert project_state(tmp_path / "崇祯书")["brain_ready"] is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_brain_ready.py -v`
Expected: FAIL — `ImportError: cannot import name 'brain_ready'`

- [ ] **Step 3: 实现**

`draft.py` 末尾:

```python
def brain_ready(project_root: Path) -> bool:
    """世界观/人物/卡章纲任一有实质内容 → 铺过底。旅程卡完成判定与织章拦截共用;
    成长档案(learn 的 AI 自留地)不算——那是写章之后才有的,不代表作者立过项。"""
    from .parse import is_substantive
    candidates = [project_root / CARD_REL, project_root / WORLD_REL, project_root / CHARS_REL]
    for dir_rel in (paths.WORLD_DIR_REL, paths.CHARS_DIR_REL):
        d = project_root / dir_rel
        if d.is_dir():
            candidates += [f for f in sorted(d.glob("*.md")) if f.name != paths.GROWTH_NAME]
    return any(p.is_file() and is_substantive(p.read_text(encoding="utf-8")) for p in candidates)
```

(`draft.py` 顶部已有 `from . import events, paths` 与 `CARD_REL, CHARS_REL, WORLD_REL`,无需新 import。)

`usecases.py` `project_state` 的 return 里加一行(`"brain": ...` 之前):

```python
        "brain_ready": brain_ready(root),   # 世界观/人物/卡章纲任一有实质内容(旅程卡/织章拦截用)
```

顶部 import 区补:`from .draft import brain_ready`(与既有 `from .draft import draft_brain as _draft_brain` 合并成一行 import)。

`server.py`:

```python
class CreateBody(BaseModel):
    name: str
    parent: str
    genre: str | None = None
    idea: str = ""        # 一句话设定(可空):存 loom.toml,AI 铺设定底稿的种子
    platform: str = ""    # 目标平台(可空):写立项卡平台行,定违禁词自检基线
```

```python
        root = scaffold_init(b.name, Path(b.parent).expanduser(), b.genre,
                             idea=b.idea, platform=b.platform)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_brain_ready.py tests/test_usecases.py -v`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add loom/draft.py loom/usecases.py loom/server.py tests/test_brain_ready.py
git commit -m "feat(server): create 收 idea/platform;state 暴露 brain_ready(铺底判定单一真相)"
```

---

### Task 9: webui——建书框两个可选字段 + 建书后自动铺底稿

**Files:**
- Modify: `loom/webui/index.html:264-282`(create-overlay)
- Modify: `loom/webui/app.js:271-281`(`createProject`)

**Interfaces:**
- Consumes: `POST /api/project/create` 的 `idea`/`platform`(Task 8);既有 `draftBrain(idea)`(app.js:785,内部自带后端就绪检查与 toast)、`providerKeyed()`。

- [ ] **Step 1: index.html——「题材」select 之后、`.row-end` 之前插入**

```html
      <label class="field-label">一句话设定(可选,AI 铺设定底稿按它起草)</label>
      <textarea id="new-idea" class="draft-idea" placeholder="例:重生成崇祯,开局砍魏忠贤,拯救大明。留空=按书名+题材发挥。"></textarea>
      <label class="field-label">目标平台(可选,定违禁词自检的松紧)</label>
      <select id="new-platform">
        <option value="">不选</option>
        <option>起点</option><option>番茄</option><option>晋江</option><option>七猫</option>
      </select>
```

(`draft-idea` 是既有样式类——起草弹层的同款输入框,直接复用,不加新 CSS。)

- [ ] **Step 2: app.js——createProject 传参 + 自动铺底稿**

替换 `createProject`:

```js
async function createProject() {
  $("create-error").textContent = "";
  const name = $("new-name").value.trim();
  if (!name) { $("create-error").textContent = "先给这本书起个名字。"; $("new-name").focus(); return; }
  const idea = $("new-idea").value.trim();
  try {
    const d = await jreq("POST", "/api/project/create",
      { name, parent: $("new-parent").value.trim(), genre: $("new-genre").value || null,
        idea, platform: $("new-platform").value || "" });
    $("create-overlay").classList.add("hidden");
    enterProject(d);
    // 填了设定=明确意图:模型就绪就直接铺底稿(仅建书这一次;draftBrain 自带就绪检查与 toast)
    if (idea && providerKeyed((d.backend || {}).provider)) draftBrain("");
  } catch (e) { $("create-error").textContent = e.message; }
}
```

(`draftBrain("")` 空 idea → server 端回退读 loom.toml 的 idea,Task 7 已落。)

- [ ] **Step 3: 语法检查**

Run: `node --check loom/webui/app.js`
Expected: 无输出(通过)

- [ ] **Step 4: Commit**

```bash
git add loom/webui/index.html loom/webui/app.js
git commit -m "ux(create): 建书框收一句话设定/平台;填了设定+模型就绪 → 建书即自动铺设定底稿"
```

---

### Task 10: webui——旅程卡四步变五步

**Files:**
- Modify: `loom/webui/app.js:587-626`(`renderJourney`)

**Interfaces:**
- Consumes: `DATA.brain_ready`(Task 8;draftBrain 成功后 `DATA = d.state` 已自动刷新它)。

- [ ] **Step 1: steps 数组「接入模型」之后插入一步,并把硬编码的 `/4` 改成动态**

```js
  const steps = [
    { label: "接入模型(填 key 或用订阅)", done: keyed, run: openSettings },
    { label: "AI 铺设定底稿(世界观/人物/卡章纲)", done: !!DATA.brain_ready, run: () => draftBrain("") },
    { label: "喂样本,让它懂你(seed)", done: DATA.fingerprint_source !== "default", run: openSeed },
    { label: "织第一章", done: (DATA.chapters || []).length > 0,
      run: () => writeChapter(DATA.next_chapter, false) },
    { label: "手改后 learn(越写越像你)", done: (DATA.chapters || []).some((c) => c.learned),
      run: () => { const c = (DATA.chapters || [])[0];
        if (c) openFile(`正文/第${c.n}章.md`, true, c.n); else toast("先织第一章"); } },
  ];
```

标题行改动态:

```js
  t.textContent = `起步 · ${n}/${steps.length}`;
```

- [ ] **Step 2: 语法检查**

Run: `node --check loom/webui/app.js`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add loom/webui/app.js
git commit -m "ux(journey): 旅程卡五步——「AI 铺设定底稿」进主流程,完成判定吃 brain_ready"
```

---

### Task 11: webui——织章拦截(外置大脑全空时弹一次)

**Files:**
- Modify: `loom/webui/app.js:7` 附近(模块级状态)与 `:1256`(`writeChapter` 开头)

**Interfaces:**
- Consumes: `DATA.brain_ready`(Task 8)、`showGuide()`(app.js:699)、`draftBrain()`。

- [ ] **Step 1: 模块级状态(文件顶部 `_rewriteSel` 一带加一行)**

```js
let _brainGateShown = false;   // 织章拦截只弹一次:「就这样写」后本次会话不再唠叨
```

- [ ] **Step 2: writeChapter 拦截(key 检查之后、`_wroteChapter = null;` 之前插入)**

```js
  // 外置大脑全空(世界观/人物/卡章纲都没实质内容)→ 拦一次:直接写必跑偏,AI 只能瞎编
  if (!DATA.brain_ready && !_brainGateShown) {
    _brainGateShown = true;
    showGuide({
      title: "设定还是空的",
      bodyHtml:
        `<p class="guide-lead">世界观 / 人物 / 卡章纲都还没内容——直接写,AI 只能按题材套路瞎编,容易和你的书名、想法对不上。</p>` +
        `<p class="hint">推荐先让 AI 按书名起草一版设定底稿(十几秒),你改成自己的再开写。</p>`,
      primary: { label: "✨ 先让 AI 铺底稿", fn: () => draftBrain("") },
      ghost: { label: "就这样写", fn: () => writeChapter(n, force) },
    });
    return;
  }
```

- [ ] **Step 3: 语法检查**

Run: `node --check loom/webui/app.js`
Expected: 通过

- [ ] **Step 4: Commit**

```bash
git add loom/webui/app.js
git commit -m "ux(write): 织章拦截——外置大脑全空时弹一次「先铺底稿」,填过任意一份即不拦"
```

---

### Task 12: 全量回归 + server 冒烟

**Files:** 无新改动(验证任务;发现问题就地修再回归)

- [ ] **Step 1: 全量测试**

Run: `.venv/bin/python -m pytest -q`
Expected: 全绿(0 failed)

- [ ] **Step 2: server 冒烟(scratchpad 里起服务,curl 走一遍新链路)**

```bash
.venv/bin/python -c "
import threading, uvicorn
from loom.server import app
uvicorn.run(app, host='127.0.0.1', port=8788, log_level='warning')
" &
sleep 2
curl -s -X POST localhost:8788/api/project/create -H 'Content-Type: application/json' \
  -d '{"name":"冒烟-崇祯书","parent":"'$SCRATCH'","idea":"重生成崇祯,开局砍魏忠贤","platform":"番茄"}'
```

(`$SCRATCH` 换成本会话 scratchpad 目录。)
Expected 响应 JSON:`"title":"冒烟-崇祯书"`、`"brain_ready":false`;且 `$SCRATCH/冒烟-崇祯书/loom.toml` 含 `idea  = "重生成崇祯,开局砍魏忠贤"`、`外置大脑/立项卡.md` 含 `平台:番茄`、`外置大脑/卡章纲.md` 无「濒死」预填行。验完 kill 后台服务、删冒烟项目目录。

- [ ] **Step 3: 汇报**

向用户汇报:改了什么、测试结果、老书一次性重跑说明(签名变更)、golden diff 审了什么。**不合并、不发版**——等用户验收。
